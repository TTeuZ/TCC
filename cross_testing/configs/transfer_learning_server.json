{
    "datasets": {
        "train_dataset": "/home/pmla20/datasets/PKLotSegmented",
        "test_dataset": "/home/pmla20/datasets/CNRParkEXTSegmented",
        "split": 0.7
    },
    "model": {
        "module": "tools.models.mobilenet_v3_large",
        "config": {
            "training_mode": "transfer",
            "normalize_data": true,
            "loss": "CrossEntropyLoss",
            "optimizer": "Adam",
            "device": "cuda:1"
        }
    },
    "experiment": {
        "type": "transfer_learning",
        "epochs": 20,
        "dl_config": {
            "img_size": [
                128,
                128
            ],
            "batch_size": 3500,
            "num_workers": 24
        }
    }
}